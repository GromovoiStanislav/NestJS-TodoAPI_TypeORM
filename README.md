## NestJS Todo API (TypeORM, Swagger, Authentication, Pipes)

Authentication:

```
npm i @nestjs/passport @nestjs/jwt passport-jwt
npm i @types/passport-jwt -D
npm i bcryptjs
npm i @types/bcryptjs -D
```